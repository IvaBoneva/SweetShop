<!DOCTYPE html>
<html lang="bg" id="edit_page">
  <head>
    <meta charset="UTF-8" />
    <title>Редактиране на продукт</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <h1>Редактирай продукт: <%= product.title %></h1>

    <form
      action="/admin/products/<%= product._id %>?_method=PUT"
      method="POST"
      id="edit_product"
    >
      <label>Име:</label><br />
      <input
        type="text"
        name="title"
        value="<%= product.title %>"
        required
      /><br />

      <label>Slug:</label><br />
      <input
        type="text"
        name="slug"
        value="<%= product.slug %>"
        required
      /><br />

      <label>Автор:</label><br />
      <input
        type="text"
        name="author"
        value="<%= product.author %>"
        required
      /><br />

      <label>Цена (лв.):</label><br />
      <input
        type="number"
        step="0.01"
        name="price"
        value="<%= parseFloat(product.price).toFixed(2) %>"
        required
      /><br />

      <label>URL на снимка:</label><br />
      <input
        type="text"
        name="image"
        value="<%= product.image %>"
        required
      /><br />

      <label>Кратко описание:</label><br />
      <textarea name="shortDescription" rows="2" required>
<%= product.shortDescription %></textarea
      ><br />

      <label>Подробно описание:</label><br />
      <textarea name="fullDescription" rows="4" required>
<%= product.fullDescription %></textarea
      ><br />

      <% for (let i = 0; i < 3; i++) { %>
      <label>Ревю <%= i + 1 %>:</label><br />
      <input
        type="text"
        name="reviews[]"
        value="<%= product.reviews[i] || '' %>"
      /><br />
      <% } %>

      <button type="submit">Запази</button>
    </form>
  </body>
</html>
